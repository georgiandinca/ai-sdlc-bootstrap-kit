# Drafts — exploratory, not authoritative

Work-in-progress that is **not** yet load-bearing. This is **Exploratory** tier (`AGENTS.md` §4.2): an AI agent does **not** read anything here unless the user names the specific file.

- Promote a draft to canonical deliberately — move it to the right `docs/<topic>/` folder via a `promote: <path>` PR and add the frontmatter contract (`WORKING-AGREEMENT.md` §4.3).
- This folder is exempt from the frontmatter contract while material is still exploratory.
