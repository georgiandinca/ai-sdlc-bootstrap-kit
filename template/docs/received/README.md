# Received — immutable input

Client / stakeholder input lands here, **unmodified**, at `docs/received/<source>/<YYYY-MM-DD>-<name>.<ext>`. This is **Authoritative** tier (`AGENTS.md` §4.2): cite directly, never edit, never paraphrase from memory.

- Record each item with a one-line entry in [`../../INDEX.md`](../../INDEX.md) (source + date).
- Source binaries (`.docx`/`.pdf`) are read with the appropriate skill; convert to Markdown for grounding if you ingest them into the knowledge layer — but keep the original here untouched.
- This folder is exempt from the frontmatter contract (the originals carry their own form).
